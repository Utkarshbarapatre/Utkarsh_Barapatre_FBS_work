1)select name,salary from Employees ordered by salary desc;
2)select name,salary from Employees ordered by salary desc LIMIT 5;
3)select * from Employees limit 5 offset 3;
4)select dep,count(*) as emp_count from Employees group by dep;
5)select dep,avg(salary) as avg_salary from Employees group by dep having avg(salary)>50000;
6)select * from Products where prod_name like '%Laptop%';
7)select substring(student_name,1,3) as three_char from student;
8)select * from student where student_name like 'A%';
9)select month(order_date) as month,count(* as total_order from Orders group by month(order_date) having count(*)>100;
10)select category,sum(sale_amt)as total_sale,avg(sale_amt) as avg_sale from sales group by category having sum(sale_amt)>100000; 
11)a) The total number of employees
12)a) 9
13)c) HAVING
14)b) Returns all rows from the left table and matching rows from the right table
15)b) HELLO WORLD
16)SELECT c.region, SUM(o.total_amount) AS total_order_amount
FROM Orders o
JOIN Customers c ON o.customer_id=c.customer_id
GROUP BY c.region
HAVING SUM(o.total_amount)>10000;
17)SELECT b.author_id, SUM(s.quantity) AS total_books_sold
FROM Books b
JOIN Sales s ON b.book_id=s.book_id
GROUP BY b.author_id
HAVING SUM(s.quantity)>100;

